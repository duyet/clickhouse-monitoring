apiVersion: v1
kind: Service
metadata:
  name: clickhouse-monitoring
  labels:
    app.kubernetes.io/name: clickhouse-monitoring
spec:
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: clickhouse-monitoring
