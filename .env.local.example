# ClickHouse Hosts (existing)
CLICKHOUSE_HOST=localhost
CLICKHOUSE_USER=default
CLICKHOUSE_PASSWORD=

# Database configuration for SSO/Auth
# Default: SQLite (./clickhouse-monitor.db)
# For production: Use PostgreSQL

# SQLite (default for local development)
DATABASE_URL=sqlite:./clickhouse-monitor.db

# PostgreSQL (production)
# DATABASE_URL=postgresql://user:password@localhost:5432/clickhouse-monitor

# LibSQL/Turso
# DATABASE_URL=libsql://your-database.turso.io?authToken=your-token

# Auto-run migrations on startup (default: true)
AUTO_MIGRATE=true
