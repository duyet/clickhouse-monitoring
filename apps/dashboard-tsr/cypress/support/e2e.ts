// Loaded automatically before every E2E spec. Kept intentionally minimal —
// the TSR app's specs are framework-agnostic smoke/parity checks with no
// custom commands or API mocks (unlike the Next app's support file).
export {}
