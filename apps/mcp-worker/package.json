{
  "name": "mcp-worker",
  "description": "Standalone Cloudflare Worker for the chmonitor MCP endpoint",
  "version": "0.2.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy --minify",
    "dev": "wrangler dev",
    "typegen": "wrangler types"
  },
  "dependencies": {
    "@chm/mcp-server": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.29.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260515.0",
    "wrangler": "4.65.0"
  }
}
