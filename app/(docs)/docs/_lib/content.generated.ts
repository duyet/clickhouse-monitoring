// Generated by scripts/build-docs-content.ts. Do not edit by hand.
export const docsContent: Record<string, string> = {
  "advanced/custom-name": "import { Tabs } from 'nextra/components'\n\n# Custom Name\n\n![](/custom-name.png)\n\nYou can customize the ClickHouse host name by using the environment variable below:\n\n- `CLICKHOUSE_NAME`: A list of names for the ClickHouse instances separated by commas. The number of items must match the number of hosts in `CLICKHOUSE_HOST`. For example `clickhouse-0,clickhouse-1,clickhouse-2`.\n",
  "advanced/feature-permissions": "# Feature Permissions\n\nFeature permissions let each deployment hide or protect specific surfaces without changing the application code.\n\nOn self-hosted deployments without Clerk, every feature is public and enabled —\nno configuration needed. Just set your ClickHouse connection and the dashboard\nworks. The defaults are:\n\n```text\nenabled = true\naccess = \"public\"     (alias: \"guest\")\n```\n\nOnly configure features that need different behavior. Missing entries keep the\ndefault.\n\n## Access Values\n\n| Value | Behavior |\n| --- | --- |\n| `public` | Guests and signed-in users can see and use the feature. |\n| `guest` | Alias for `public`. |\n| `authenticated` | Requires a Clerk session. Agent APIs also accept a valid `AGENT_API_TOKEN`. |\n\nV1 only supports public, including the `guest` alias, and authenticated access.\nThere are no roles.\n\n## Config File\n\nSet `CHM_CONFIG_FILE` to a TOML or YAML file path.\n\n```toml\n# /etc/clickhouse-monitor/config.toml\n[features.agent]\nenabled = true\naccess = \"authenticated\"\n\n[features.metrics]\nenabled = true\naccess = \"guest\"\n\n[features.settings]\nenabled = false\n```\n\nThe same config in YAML:\n\n```yaml\nfeatures:\n  agent:\n    enabled: true\n    access: authenticated\n  metrics:\n    enabled: true\n    access: guest\n  settings:\n    enabled: false\n```\n\n## Docker\n\nMount the config file and point `CHM_CONFIG_FILE` at the mounted path:\nReplace `vX.Y.Z` with the release tag you want to run.\n\n```bash\ndocker run \\\n  -e CLICKHOUSE_HOST='http://clickhouse:8123' \\\n  -e CLICKHOUSE_USER='default' \\\n  -e CLICKHOUSE_PASSWORD='' \\\n  -e CHM_CONFIG_FILE='/etc/clickhouse-monitor/config.toml' \\\n  -v \"$PWD/config.toml:/etc/clickhouse-monitor/config.toml:ro\" \\\n  ghcr.io/duyet/clickhouse-monitoring:vX.Y.Z\n```\n\nFor env-only Docker config:\n\n```bash\ndocker run \\\n  -e CLICKHOUSE_HOST='http://clickhouse:8123' \\\n  -e CLICKHOUSE_USER='default' \\\n  -e CLICKHOUSE_PASSWORD='' \\\n  -e CHM_FEATURE_AGENT_ACCESS='authenticated' \\\n  -e CHM_FEATURE_SETTINGS_ENABLED='false' \\\n  ghcr.io/duyet/clickhouse-monitoring:vX.Y.Z\n```\n\n## Environment Overrides\n\nEnvironment variables override the config file.\n\n```bash\n# Disable many features at once\nCHM_DISABLED_FEATURES=settings,insights\n\n# Require auth for many features at once\nCHM_AUTH_REQUIRED_FEATURES=agent,settings\n\n# Override one feature\nCHM_FEATURE_METRICS_ENABLED=false\nCHM_FEATURE_AGENT_ACCESS=authenticated\nCHM_FEATURE_METRICS_ACCESS=guest\n```\n\nFeature names are case-insensitive in the config file. Env variable names use uppercase feature ids.\n\n## Authentication\n\nAuthenticated feature access needs Clerk enabled:\n\n```bash\nCHM_AUTH_PROVIDER=clerk\nNEXT_PUBLIC_AUTH_PROVIDER=clerk\nNEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_live_your_key\nCLERK_SECRET_KEY=sk_live_your_key\n```\n\nFor agent APIs, `access = \"authenticated\"` accepts either a signed-in Clerk browser session or:\n\n```bash\nAGENT_API_TOKEN=your-shared-token\n```\n\nThen call the agent API with:\n\n```bash\nAuthorization: Bearer your-shared-token\n```\n\n## Common Policies\n\nAllow guests to use everything except the AI agent:\n\n```toml\n[features.agent]\naccess = \"authenticated\"\n```\n\nHide metrics completely:\n\n```toml\n[features.metrics]\nenabled = false\n```\n\nMake settings visible only after sign-in:\n\n```toml\n[features.settings]\naccess = \"authenticated\"\n```\n\n## Supported Features\n\nUse these feature ids in TOML, YAML, or env names:\n\n```text\noverview\nagent\ninsights\nhealth\nqueries\ntables\nmetrics\ndashboard\nsecurity\nlogs\nsettings\ncluster\noperations\nactions\nmcp\ndocs\nabout\n```\n\n## Precedence\n\nWhen multiple sources set the same feature, later sources win:\n\n```text\nbuilt-in allow-all defaults\nTypeScript config defaults\nCHM_CONFIG_FILE TOML/YAML\nenvironment variables\n```\n\nDisabled features are removed from navigation and command search. Direct page visits show a disabled screen. API routes return a blocked response before running ClickHouse queries or agent logic.\n",
  "advanced/multiple-hosts": "import { Tabs } from 'nextra/components'\n\n# Multiple Hosts\n\n![](/multiple-hosts.png)\n\nYou can configure the UI to have multiple ClickHouse hosts by using these environment variables:\n\n- `CLICKHOUSE_HOST`: Separated by commas, for example: `http://ch-1:8123,http://ch-2:8123`\n- `CLICKHOUSE_NAME`: (Optional) Name of the ClickHouse instance, must match the number of hosts in `CLICKHOUSE_HOST`, for example `ch-1,ch-2`.\n\nAll these hosts will share the same `CLICKHOUSE_USER` and `CLICKHOUSE_PASSWORD`. If you want to have multiple users/passwords for each host, specify them in the usual way:\n\n- `CLICKHOUSE_USER`: `user1,user2`\n- `CLICKHOUSE_PASSWORD`: `password1,password2`\n\n# Examples Docker\n\nReplace `vX.Y.Z` with the release tag you want to run.\n\n<Tabs items={['Docker (same user)', 'Docker (different user)']}>\n  <Tabs.Tab>\n    ```bash\n    docker run -it \\\n      -e CLICKHOUSE_HOST='http://ch-1:8123,http://ch-2:8123' \\\n      -e CLICKHOUSE_NAME='ch-1,ch-2' \\\n      -e CLICKHOUSE_USER='default' \\\n      -e CLICKHOUSE_PASSWORD='' \\\n      --name clickhouse-monitoring \\\n    ghcr.io/duyet/clickhouse-monitoring:vX.Y.Z\n    ```\n  </Tabs.Tab>\n  <Tabs.Tab>\n    ```bash\n    docker run -it \\\n      -e CLICKHOUSE_HOST='http://ch-1:8123,http://ch-2:8123' \\\n      -e CLICKHOUSE_NAME='ch-1,ch-2' \\\n      -e CLICKHOUSE_USER='user1,user2' \\\n      -e CLICKHOUSE_PASSWORD='password1,password2' \\\n      --name clickhouse-monitoring \\\n    ghcr.io/duyet/clickhouse-monitoring:vX.Y.Z\n    ```\n  </Tabs.Tab>\n</Tabs>\n\n# Examples Kubernetes\n\n<Tabs items={['Kubernetes (same user)', 'Kubernetes (different user)']}>\n  <Tabs.Tab>\n    ```bash\n    helm repo add duyet https://duyet.github.io/charts\n    \n    cat <<EOF >> values.yaml\n    env:\n    - name: CLICKHOUSE_HOST\n      value: http://ch-1:8123,http://ch-2:8123\n    - name: CLICKHOUSE_NAME\n      value: ch-1,ch-2\n    - name: CLICKHOUSE_USER\n      value: default\n    - name: CLICKHOUSE_PASSWORD\n      value: ''\n    EOF\n    \n    helm install -f values.yaml clickhouse-monitoring-release duyet/clickhouse-monitoring\n    ```\n </Tabs.Tab>\n <Tabs.Tab>\n    ```bash\n    helm repo add duyet https://duyet.github.io/charts\n    \n    cat <<EOF >> values.yaml\n    env:\n    - name: CLICKHOUSE_HOST\n      value: http://ch-1:8123,http://ch-2:8123\n    - name: CLICKHOUSE_NAME\n      value: ch-1,ch-2\n    - name: CLICKHOUSE_USER\n      value: user1,user2\n    - name: CLICKHOUSE_PASSWORD\n      value: password1,password2\n    EOF\n    \n    helm install -f values.yaml clickhouse-monitoring-release duyet/clickhouse-monitoring\n    ```\n </Tabs.Tab>\n</Tabs>\n",
  "advanced/peerdb-monitoring": "# PeerDB Monitoring\n\nCHM includes an optional, **view-only** PeerDB section (Mirrors and Peers) that\nsurfaces replication status, throughput, lag, and per-mirror detail from a\n[PeerDB](https://docs.peerdb.io) deployment. CHM never mutates PeerDB — it\nproxies a read-only allowlist of the PeerDB REST API.\n\n> Open it at `/peerdb` once configured. The section is hidden until\n> `PEERDB_API_URL` is set.\n\n---\n\n## Enable\n\nSet `PEERDB_API_URL` (and `PEERDB_PASSWORD` if your PeerDB API requires auth),\nthen restart the app.\n\n```bash\n# PeerDB UI behind NextAuth — include the /api suffix\nPEERDB_API_URL=https://peerdb.example.com/api\nPEERDB_PASSWORD=your-peerdb-ui-password\n\n# OR a raw flow-api with no auth — use the bare origin\nPEERDB_API_URL=http://localhost:8113\n```\n\n| Variable | Default | Description |\n|---|---|---|\n| `PEERDB_API_URL` | — | Base URL of the PeerDB REST API. For the PeerDB **UI** (NextAuth) include the `/api` suffix; for a raw **flow-api** use the bare origin (e.g. `http://host:8113`). |\n| `PEERDB_PASSWORD` | — | Sent as HTTP Basic with an empty username (`base64(\":\" + password)`). Leave empty if the API has no auth. Server-side only — never sent to the browser. |\n| `PEERDB_CACHE_TTL_MS` | `10000` | TTL for the server-side response cache (set `0` to disable). |\n| `PEERDB_CACHE_MAX_ENTRIES` | `500` | Max cached responses before oldest entries are evicted. |\n| `PEERDB_FETCH_TIMEOUT_MS` | `10000` | Upstream request timeout. |\n\nSee the full list in [Environment Variables](/docs/reference/environment-variables).\n\n---\n\n## Connection status\n\nThe header shows a status pill that distinguishes:\n\n- **Connected** — API reachable and authenticated.\n- **Auth failed** — credentials rejected (check `PEERDB_PASSWORD`). For the\n  PeerDB UI this is the **UI login password**.\n- **Unreachable** — wrong `PEERDB_API_URL` or a network/DNS issue.\n- **Not configured** — `PEERDB_API_URL` is unset.\n\n---\n\n## Security\n\nCHM proxies only a **read-only allowlist** of PeerDB endpoints\n(`app/api/v1/peerdb/[...slug]`). Mutating endpoints (create/drop/pause, alert\nconfig, maintenance) are rejected with `403`. The PeerDB credential is attached\nserver-side and never reaches the browser bundle, and secret-shaped peer config\nfields are masked in the UI.\n\nThe section also respects [Feature Permissions](/docs/advanced/feature-permissions)\n— gate it with `CHM_FEATURE_PEERDB_ACCESS=authenticated` or disable it with\n`CHM_FEATURE_PEERDB_ENABLED=false`. The proxy enforces the same gate, so it\ncannot be reached directly when the feature is disabled or restricted.\n\n---\n\n## Local development (mock)\n\nTo preview the full UI without a real PeerDB instance, run the bundled mock\nserver:\n\n```bash\nbun run peerdb:mock                                  # serves :8113\nPEERDB_API_URL=http://localhost:8113 bun run dev     # → /peerdb\n```\n\n---\n\n## Troubleshooting\n\n| Symptom | Likely cause |\n|---|---|\n| Pill shows **Auth failed** | Wrong `PEERDB_PASSWORD`. For the PeerDB UI, use the UI login password; for a raw flow-api, match its configured password (or leave empty). |\n| Pill shows **Unreachable** | `PEERDB_API_URL` host/port wrong, or the API is not reachable from the CHM server. |\n| Mirrors load but charts are empty | The mirror has no recent CDC graph/batch data yet, or the PeerDB version doesn't expose those endpoints. |\n| Large fleets show partial KPI totals | Per-row metrics load lazily above 24 mirrors; the Throughput/Rows-synced cards label how many mirrors are loaded. Expand a row to load its metrics. |\n",
  "advanced/queries-history": "import { Tabs } from 'nextra/components'\n\n# Query History\n\n## 1. Excluding monitoring users by default\n\n![](/excluding-users.png)\n\nYou can configured the [`/history-queries`](http://clickhouse-monitoring.vercel.app/0/history-queries) page to filter out the `monitoring` users by default.\nTo do this, using these environment variables:\n\n- `CLICKHOUSE_EXCLUDE_USER_DEFAULT`: for example `monitoring,healthcheck`.\n\nExamples\n\nReplace `vX.Y.Z` with the release tag you want to run.\n\n<Tabs items={['Docker', 'Kubernetes']}>\n  <Tabs.Tab>\n    ```bash\n    docker run -it \\\n        -e CLICKHOUSE_HOST='http://ch-1:8123' \\\n        -e CLICKHOUSE_NAME='ch-1' \\\n        -e CLICKHOUSE_USER='monitoring' \\\n        -e CLICKHOUSE_PASSWORD='' \\\n        -e CLICKHOUSE_EXCLUDE_USER_DEFAULT='monitoring,healthcheck' \\\n        --name clickhouse-monitoring \\\n    ghcr.io/duyet/clickhouse-monitoring:vX.Y.Z\n    ```\n  </Tabs.Tab>\n  <Tabs.Tab>\n    ```bash\n    helm repo add duyet https://duyet.github.io/charts\n    \n    cat <<EOF >> values.yaml\n    env:\n    - name: CLICKHOUSE_HOST\n      value: http://ch-1:8123\n    - name: CLICKHOUSE_NAME\n      value: ch-1\n    - name: CLICKHOUSE_USER\n      value: monitoring\n    - name: CLICKHOUSE_PASSWORD\n      value: ''\n    - name: CLICKHOUSE_EXCLUDE_USER_DEFAULT\n      value: monitoring,healthcheck\n    EOF\n    \n    helm install -f values.yaml clickhouse-monitoring-release duyet/clickhouse-monitoring\n    ```\n  </Tabs.Tab>\n</Tabs>\n",
  "advanced/self-tracking": "# Dashboard Self Tracking\n\nThe monitoring dashboard tracks user behavior by default. It's useful for debugging and improving the dashboard.\n\nUse these environment variables to configure the dashboard's self-tracking:\n\n- `EVENTS_TABLE_NAME`: The table name for storing dashboard self-tracking events.\n    Default: [`system.monitoring_events`](https://clickhouse-monitoring.vercel.app/0/database/system/monitoring_events)\n\n![](/self-tracking-1.png)\n\n![](/self-tracking-2.png)",
  "ai-agent": "# AI Agent\n\nThe AI Agent turns natural language into ClickHouse insights. Ask a question in\nplain English and the agent plans a series of tool calls — running read-only\nSQL, inspecting `system.*` tables, comparing hosts, spotting issues — then\nsummarizes the findings and can render charts inline.\n\nThe agent connects through the **same configured ClickHouse host and user** as\nthe rest of the dashboard, so its visibility is limited by the grants you give\nthat user. By default it only runs read-only queries.\n\n> Open the agent at `/agents` (use the host selector or `?host=0` to pick a\n> host). It is available on any deployment that has an LLM provider configured.\n\n## How It Works\n\n1. You send a message (optionally targeting a specific `hostId`).\n2. The agent calls one or more **tools** to gather evidence — each tool is a\n   purpose-built, version-aware query against ClickHouse system tables.\n3. When a question needs deeper expertise, the agent loads a **skill** — a\n   bundled expert guide — to inform its analysis before answering.\n4. It streams back a concise answer, shows the SQL it ran, and suggests\n   follow-ups or visualizations.\n\nThe agent runs in a bounded tool loop: it keeps calling tools until it has\nenough to answer, then stops. Destructive actions (kill query, optimize) are\ndisabled unless explicitly enabled (see [Configuration](#configuration)).\n\n## Configuration\n\nThe agent uses an OpenAI-compatible API. At minimum set `LLM_API_KEY`.\n\n```bash\nLLM_API_KEY=your-provider-key\nLLM_API_BASE=https://openrouter.ai/api/v1   # default\nLLM_MODEL=openrouter:openrouter/free        # default\n```\n\n| Variable | Default | Purpose |\n|---|---|---|\n| `LLM_API_KEY` | — | Provider API key (required). |\n| `LLM_API_BASE` | `https://openrouter.ai/api/v1` | OpenAI-compatible base URL. |\n| `LLM_MODEL` | `openrouter:openrouter/free` | Model identifier. |\n| `AGENT_API_TOKEN` | — | Shared Bearer token for the agent API. |\n| `AGENT_ENABLE_CONTROL_TOOLS` | `false` | Enables destructive tools (kill query, optimize). |\n\nFor public deployments, require authentication for the agent:\n\n```bash\nCHM_FEATURE_AGENT_ACCESS=authenticated\n```\n\nSee [Environment Variables](/docs/reference/environment-variables) and\n[Feature Permissions](/docs/advanced/feature-permissions) for the full list.\n\n## Tools\n\nTools are the functions the agent calls to gather data. Every tool accepts an\noptional `hostId` (a numeric, 0-based index — `0`, `1`, `2`) and defaults to the\ncurrent host. The agent picks tools automatically; you never call them directly.\n\n### Schema & Exploration\n\n| Tool | Description |\n|---|---|\n| `query` | Execute a read-only SQL query (SELECT, WITH/CTE, DESCRIBE). |\n| `list_databases` | List all databases. |\n| `list_tables` | List tables in a database with sizes and row counts. |\n| `get_table_schema` | Column definitions for a table. |\n| `explore_table_schema` | Schema exploration with relationship discovery. |\n| `discover_data_sources` | Find tables/columns relevant to a topic. |\n\n### Query Analysis\n\n| Tool | Description |\n|---|---|\n| `get_running_queries` | Currently executing queries with elapsed time. |\n| `get_slow_queries` | Slowest completed queries. |\n| `get_failed_queries` | Recent failed queries with error details. |\n| `get_expensive_queries` | Top queries by memory, bytes read, or duration. |\n| `get_query_patterns` | Aggregated query fingerprints with frequency. |\n| `explain_query` | EXPLAIN plan / pipeline / indexes for a query. |\n| `analyze_query_optimization` | Optimization opportunities for a SQL query. |\n| `repair_query` | Self-fix a broken or slow read-only query. |\n| `spot_issues` | Scan recent history for likely problems. |\n\n### System Health\n\n| Tool | Description |\n|---|---|\n| `get_metrics` | Server version, uptime, connections, memory. |\n| `get_system_resources` | CPU, memory, disk, network, thread metrics. |\n| `get_disk_usage` | Per-disk free/total/used percentage. |\n| `get_errors` | Recent system errors with counts. |\n| `get_crash_log` | Server crash history. |\n| `detect_anomalies` | Compare recent vs baseline metrics for anomalies. |\n| `generate_health_report` | Collect all key metrics into one report. |\n| `investigate_incident` | Automated root-cause analysis for an incident. |\n\n### Storage, Merges & Mutations\n\n| Tool | Description |\n|---|---|\n| `get_table_parts` | Part-level sizes, rows, compression ratio. |\n| `get_detached_parts` | Detached parts needing attention. |\n| `get_top_tables_by_size` | Top tables by compressed size. |\n| `get_merge_status` | Active merge operations with progress. |\n| `get_mutations` | Pending and stuck mutations. |\n| `get_merge_performance` | Historical merge throughput. |\n\n### Replication & Cluster\n\n| Tool | Description |\n|---|---|\n| `get_replication_status` | Per-table lag, queue size, leader/readonly. |\n| `get_replication_queue` | Pending replication tasks. |\n| `get_clusters` | Cluster topology: shards, replicas, hosts. |\n| `get_distributed_ddl_queue` | Pending/failed distributed DDL operations. |\n| `get_zookeeper_info` | ZooKeeper/Keeper node data. |\n\n### Security & Audit\n\n| Tool | Description |\n|---|---|\n| `get_active_sessions` | Current sessions with user/client info. |\n| `get_login_attempts` | Recent login successes/failures. |\n| `get_users_and_roles` | Users, roles, and access grants. |\n\n### Schema Migration\n\n| Tool | Description |\n|---|---|\n| `analyze_schema_change` | Assess impact of a proposed ALTER TABLE. |\n| `get_column_usage` | Find queries referencing a column (blast radius). |\n| `recommend_table_design` | Suggest ORDER BY, types, indexes, MVs. |\n\n### Comparison, Insights & Visualization\n\n| Tool | Description |\n|---|---|\n| `compare_time_periods` | Compare metrics between two time periods. |\n| `compare_hosts` | Compare two hosts side-by-side. |\n| `get_query_insights` | Highlight stats: largest scans, peak memory, etc. |\n| `get_table_insights` | Table-level insights: size, rows, compression. |\n| `forecast_capacity` | Forecast days-until-disk-full from 30-day trends. |\n| `query_and_visualize` | Run SQL and return an interactive chart config. |\n\n### Settings, Logs & Navigation\n\n| Tool | Description |\n|---|---|\n| `get_settings` | Server settings changed from defaults. |\n| `get_mergetree_settings` | MergeTree settings changed from defaults. |\n| `get_text_log` | Server log entries by level and pattern. |\n| `get_stack_traces` | Current thread stack traces. |\n| `get_dashboard_pages` | List dashboard pages and routes. |\n| `get_chart_data` | Fetch data from a specific dashboard chart. |\n\n### Control Actions (destructive)\n\nDisabled unless `AGENT_ENABLE_CONTROL_TOOLS=true`. The agent always confirms\nbefore using them.\n\n| Tool | Description |\n|---|---|\n| `optimize_table` | Trigger `OPTIMIZE TABLE`. |\n| `kill_query` | Kill a running query by id. |\n\n### Interaction & Knowledge\n\n| Tool | Description |\n|---|---|\n| `ask_user` | Ask a structured clarifying question. |\n| `load_skill` | Load a specialized expert guide (see Skills below). |\n\n## Skills\n\nSkills are bundled expert guides the agent loads on demand to deepen its\nanswers. They live in `.agents/skills/<name>/SKILL.md` and are compiled into a\nregistry. The agent loads the relevant skill before answering domain questions.\n\n| Skill | Covers |\n|---|---|\n| `clickhouse-best-practices` | Schema design, query tuning, operational guidelines. |\n| `query-optimization` | PREWHERE, JOIN patterns, materialized views, EXPLAIN, indexes. |\n| `system-tables-reference` | Exact columns of `system.processes`, `query_log`, `parts`, `merges`, `replicas`, `metrics`; when to prefer dedicated tools over raw SQL. |\n| `troubleshooting` | OOM, slow merges, stuck mutations, error-code diagnosis. |\n| `replication-guide` | ReplicatedMergeTree, failover, lag diagnosis, Keeper. |\n| `cluster-operations` | Distributed tables, resharding, node management, topology. |\n| `storage-optimization` | Compression codecs, TTL, tiered storage, part management. |\n| `migration-patterns` | Schema migrations, ALTER patterns, zero-downtime changes. |\n| `security-hardening` | RBAC, row policies, quotas, audit logging. |\n\nThe available skills are exposed at `GET /api/v1/agent/skills`.\n\n## Examples\n\nAsk questions the way you would ask a teammate:\n\n- **\"Which queries are running right now and how long have they been executing?\"**\n  The agent calls `get_running_queries` and lists query id, user, elapsed time,\n  and memory, sorted by duration.\n\n- **\"What were the 10 slowest queries in the last 24 hours?\"**\n  Uses `get_slow_queries`, then offers to `explain_query` on any of them.\n\n- **\"Why is the cluster slow right now?\"**\n  Loads the `troubleshooting` skill, runs `spot_issues` and `detect_anomalies`,\n  and correlates merges, errors, and expensive queries into a root-cause summary.\n\n- **\"Show me query volume per hour over the last day as a chart.\"**\n  Uses `query_and_visualize` to run the aggregation and render a line chart.\n\n- **\"Compare disk usage and query load between host 0 and host 1.\"**\n  Calls `compare_hosts` with `hostId1: 0` and `hostId2: 1`.\n\n- **\"Is it safe to drop the `event_payload` column from `analytics.events`?\"**\n  Runs `get_column_usage` to assess blast radius and `analyze_schema_change`\n  to classify the ALTER risk before recommending.\n\n- **\"Forecast when our biggest disk will fill up.\"**\n  Uses `forecast_capacity` on 30-day storage trends.\n\n## API\n\nThe agent is also available over HTTP for integrations.\n\n```bash\ncurl -X POST https://your-host/api/v1/agent \\\n  -H \"Content-Type: application/json\" \\\n  -H \"Authorization: Bearer $AGENT_API_TOKEN\" \\\n  -d '{\n    \"message\": \"Which queries are running right now?\",\n    \"hostId\": 0\n  }'\n```\n\n`hostId` may be a number or a numeric string; it defaults to `0`. List loadable\nskills with `GET /api/v1/agent/skills`.\n\n## Security Notes\n\n- The agent inherits the configured ClickHouse user's grants — scope that user\n  to read-only system access for safe exposure.\n- Keep destructive control tools off (`AGENT_ENABLE_CONTROL_TOOLS=false`) unless\n  the ClickHouse user is trusted and you intend to allow kill/optimize.\n- Require authentication on public deployments with\n  `CHM_FEATURE_AGENT_ACCESS=authenticated`.\n- Keep LLM keys server-side — never in `NEXT_PUBLIC_` variables.\n",
  "deploy": "import { Cards } from 'nextra/components'\nimport { Triangle, Container, Blocks, CheckCircle2 } from 'lucide-react'\n\n# Deployments\n\nChoose the target that matches how you operate ClickHouse. Use Docker for a\nsingle self-hosted service, Cloudflare Workers for edge hosting, Kubernetes for\ncluster-managed deployments, and Vercel for quick hosted previews.\n\n<Cards>\n  <Cards.Card icon={<Triangle />} title=\"Vercel\" href=\"/deploy/vercel\" arrow />\n  <Cards.Card\n    icon={<Triangle />}\n    title=\"Cloudflare\"\n    href=\"/deploy/cloudflare\"\n    arrow\n  />\n  <Cards.Card icon={<Container />} title=\"Docker\" href=\"/deploy/docker\" arrow />\n  <Cards.Card icon={<Blocks />} title=\"Kubernetes\" href=\"/deploy/k8s\" arrow />\n  <Cards.Card\n    icon={<CheckCircle2 />}\n    title=\"Production Checklist\"\n    href=\"/deploy/production-checklist\"\n    arrow\n  />\n</Cards>\n",
  "deploy/cloudflare": "# Cloudflare\n\nDeploy to Cloudflare Workers using the OpenNext Cloudflare adapter.\n\n## Prerequisites\n\n- Cloudflare account\n- Bun installed locally or in CI\n- Required Cloudflare credentials configured in CI when deploying from GitHub:\n  - `CLOUDFLARE_API_TOKEN` - API token with Workers deploy permissions\n  - `CLOUDFLARE_ACCOUNT_ID` - Your Cloudflare account ID\n\n## Architecture\n\nThe application uses [@opennextjs/cloudflare](https://opennext.js.org/cloudflare) to adapt Next.js for Cloudflare Workers:\n\n- **Build Process**: `bun run cf:build` generates the OpenNext output\n- **Entry Point**: `.open-next/worker.js` (Cloudflare Worker)\n- **Static Assets**: `.open-next/assets/` (served via Cloudflare CDN)\n- **Configuration**: `wrangler.toml` and `open-next.config.ts`\n\n## Local Development\n\n```bash\n# Install dependencies\nbun install\n\n# Run development server\nbun run dev\n\n# Build for Cloudflare\nbun run cf:build\n\n# Preview locally with Cloudflare environment\nbun run cf:preview\n```\n\n## Deployment Methods\n\n### 1. Automated Deployment (Recommended)\n\nGitHub Actions can deploy from the repository once Cloudflare credentials and\nClickHouse secrets are configured.\n\nSee: `.github/workflows/cloudflare.yml`\n\n### 2. Manual Deployment via CLI\n\n```bash\n# Set Worker secrets from .env.local\nbun run cf:config\n\n# Deploy to Cloudflare\nbun run cf:deploy\n```\n\n### 3. Cloudflare Dashboard or CI\n\nIf configuring a build command in Cloudflare or another CI runner:\n\n```bash\nbun install --frozen-lockfile\nbun run cf:build\n```\n\nUse Node.js 22.22.1 or newer when a Node runtime is required by the runner.\n\n## Configuration\n\n### wrangler.toml\n\nMain Cloudflare Workers configuration:\n- Entry point: `.open-next/worker.js`\n- Assets directory: `.open-next/assets`\n- Node.js compatibility enabled\n- R2, D1, KV, and Durable Objects bindings configured\n\n### open-next.config.ts\n\nOpenNext Cloudflare adapter configuration:\n- Incremental cache: KV namespace\n- Tag cache: D1 database\n- Queue: Durable Objects\n- Cache interception enabled\n\n## Environment Variables\n\nConfigure these as Worker secrets or environment variables:\n\n- `CLICKHOUSE_HOST` - ClickHouse server URL(s), comma-separated for multi-host\n- `CLICKHOUSE_USER` - Authentication username(s), comma-separated for multi-host\n- `CLICKHOUSE_PASSWORD` - Authentication password(s), comma-separated for multi-host\n- `CLICKHOUSE_NAME` - Optional instance name(s), comma-separated for multi-host\n- `CLICKHOUSE_MAX_EXECUTION_TIME` - Query timeout in seconds\n\n## Bindings\n\nThe application requires these Cloudflare resources:\n\n### KV Namespace\n- **Binding**: `NEXT_INC_CACHE_KV`\n- **Purpose**: Incremental static regeneration cache\n\n### D1 Database\n- **Binding**: `NEXT_TAG_CACHE_D1`\n- **Database**: `clickhouse-monitor`\n- **Purpose**: Next.js cache tags storage\n\n### R2 Bucket\n- **Binding**: `NEXT_INC_CACHE_R2_BUCKET`\n- **Bucket**: `clickhouse-monitoring-inc-cache`\n- **Purpose**: Large cache objects storage\n\n### Durable Objects\n- **Binding**: `NEXT_CACHE_DO_QUEUE`\n- **Class**: `DOQueueHandler`\n- **Purpose**: Background task processing\n\n## Troubleshooting\n\n### \"The entry-point file at '.open-next/worker.js' was not found\"\n\n**Cause**: The build command did not run `bun run cf:build` to generate the `.open-next/` directory.\n\n**Solutions**:\n1. Ensure the build command is `bun install --frozen-lockfile && bun run cf:build`\n2. Check that GitHub Actions workflow runs successfully\n3. Verify `.open-next/` is not in `.gitignore` (it should be, but Cloudflare builds it fresh)\n\n### Build Failures\n\n1. Check Node.js version is 22.22.1 or higher\n2. Ensure Bun is installed\n3. Review build logs for missing dependencies or configuration issues\n\n### Runtime Errors\n\n1. Verify all environment variables are set correctly\n2. Check Cloudflare bindings (KV, D1, R2, DO) are created and configured\n3. Review Cloudflare Workers logs for detailed error messages\n\n## Resources\n\n- [OpenNext Cloudflare Documentation](https://opennext.js.org/cloudflare)\n- [Cloudflare Pages Documentation](https://developers.cloudflare.com/pages/)\n- [Cloudflare Workers Documentation](https://developers.cloudflare.com/workers/)\n- [Next.js Deployment Guide](https://nextjs.org/docs/deployment)\n\nimport { Cards } from 'nextra/components'\nimport { Triangle, Container, Blocks } from 'lucide-react'\n\n<Cards>\n  <Cards.Card icon={<Triangle />} title=\"Vercel\" href=\"/deploy/vercel\" arrow />\n  <Cards.Card icon={<Container />} title=\"Docker\" href=\"/deploy/docker\" arrow />\n  <Cards.Card icon={<Blocks />} title=\"Kubernetes\" href=\"/deploy/k8s\" arrow />\n</Cards>\n",
  "deploy/docker": "# Docker\n\nUse the published image from GitHub Container Registry. Pin a release tag in\nproduction; use `:main` only when you intentionally want the rolling image.\nhttps://github.com/duyet/clickhouse-monitoring/pkgs/container/clickhouse-monitoring\n\nReplace `vX.Y.Z` with the release tag you want to run.\n\n```bash\ndocker run --rm -it \\\n  -p 3000:3000 \\\n  --add-host=host.docker.internal:host-gateway \\\n  -e CLICKHOUSE_HOST='http://host.docker.internal:8123' \\\n  -e CLICKHOUSE_USER='monitoring' \\\n  -e CLICKHOUSE_PASSWORD='change-me' \\\n  -e CLICKHOUSE_TZ='UTC' \\\n  -e CLICKHOUSE_MAX_EXECUTION_TIME='15' \\\n  -e NEXT_QUERY_CACHE_TTL='300' \\\n  --name clickhouse-monitoring \\\n  ghcr.io/duyet/clickhouse-monitoring:vX.Y.Z\n```\n\nOpen `http://localhost:3000` after the container starts.\n\n## Docker Compose\n\n```yaml\nservices:\n  clickhouse-monitoring:\n    image: ghcr.io/duyet/clickhouse-monitoring:vX.Y.Z\n    ports:\n      - '3000:3000'\n    environment:\n      CLICKHOUSE_HOST: 'http://clickhouse:8123'\n      CLICKHOUSE_USER: 'monitoring'\n      CLICKHOUSE_PASSWORD: 'change-me'\n      CLICKHOUSE_TZ: 'UTC'\n      CLICKHOUSE_MAX_EXECUTION_TIME: '15'\n      NEXT_QUERY_CACHE_TTL: '300'\n    healthcheck:\n      test: ['CMD', 'wget', '-q', '-O', '/dev/null', 'http://localhost:3000/api/health']\n      interval: 30s\n      timeout: 5s\n      start_period: 20s\n      retries: 3\n```\n\nIf ClickHouse runs in the same Compose project, use the service name in\n`CLICKHOUSE_HOST`. If ClickHouse runs on the Docker host, use the host gateway\naddress for your platform.\n\n## Health Check\n\n```bash\ndocker compose exec clickhouse-monitoring \\\n  wget -q -O /dev/null http://localhost:3000/api/health\n```\n\n## Common Problems\n\n- `localhost` inside the container points at the container, not your host.\n- Empty pages usually mean the monitoring user lacks system-table grants.\n- Public deployments should protect agent, MCP, settings, and action features\n  with authentication.\n\nimport { Cards } from 'nextra/components'\nimport { Triangle, Container, Blocks } from 'lucide-react'\n\n<Cards>\n  <Cards.Card icon={<Triangle />} title=\"Vercel\" href=\"/deploy/vercel\" arrow />\n  <Cards.Card icon={<Blocks />} title=\"Kubernetes\" href=\"/deploy/k8s\" arrow />\n</Cards>\n",
  "deploy/k8s": "# Kubernetes Helm Chart\n\nUsing the latest helm chart here: https://github.com/duyet/charts/tree/master/clickhouse-monitoring\n\n```bash\nhelm repo add duyet https://duyet.github.io/charts\n\ncat <<EOF >> values.yaml\nenv:\n  - name: CLICKHOUSE_HOST\n    value: http://localhost:8123\n  - name: CLICKHOUSE_USER\n    value: default\n  - name: CLICKHOUSE_PASSWORD\n    value: ''\n  - name: CLICKHOUSE_TZ\n    value: 'Asia/Ho_Chi_Minh'\n  - name: CLICKHOUSE_MAX_EXECUTION_TIME\n    value: '15'\n  - name: NEXT_QUERY_CACHE_TTL\n    value: '86400'\nEOF\n\nhelm install -f values.yaml clickhouse-monitoring-release duyet/clickhouse-monitoring\n```\n\nimport { Cards } from 'nextra/components'\nimport { Triangle, Container, Blocks } from 'lucide-react'\n\n<Cards>\n  <Cards.Card icon={<Triangle />} title=\"Vercel\" href=\"/deploy/vercel\" arrow />\n  <Cards.Card icon={<Container />} title=\"Docker\" href=\"/deploy/docker\" arrow />\n</Cards>\n",
  "deploy/production-checklist": "# Production Checklist\n\nUse this checklist before exposing ClickHouse Monitor to a team or the public\ninternet.\n\n## ClickHouse Access\n\n- Use a dedicated monitoring user.\n- Grant only the actions you want available from the UI.\n- Set `CLICKHOUSE_MAX_EXECUTION_TIME` so runaway dashboard queries stop quickly.\n- Keep ClickHouse credentials in deployment secrets, not source code.\n\n## Application Access\n\n- Require authentication for agent, MCP, settings, and action features.\n- Keep `CHM_API_KEY_SECRET` set when exposing `/api/mcp`.\n- Avoid putting secrets in `NEXT_PUBLIC_` variables.\n- Disable features that your team does not operate yet.\n\n## Network\n\n- Prefer HTTPS for ClickHouse Cloud and public ClickHouse endpoints.\n- Keep private ClickHouse endpoints behind your own trusted network path.\n- Verify the app can reach ClickHouse from the deployment runtime, not just from\n  your laptop.\n\n## Deployment\n\nRun the same checks before shipping:\n\n```bash\nbun run lint\nbun run build\nbun run test\n```\n\nFor Cloudflare:\n\nEnsure `.env.local` contains `CLICKHOUSE_HOST`, `CLICKHOUSE_USER`, and\n`CLICKHOUSE_PASSWORD`. Add `CLICKHOUSE_NAME` when you want custom host names.\nFor multi-host deployments, use comma-separated values in the same order across\nall four variables.\n\n```bash\nbun run cf:build\nbun run cf:deploy\nbun run cf:health\n```\n\nFor Docker:\n\nEnsure `.env.local` contains `CLICKHOUSE_HOST`, `CLICKHOUSE_USER`, and\n`CLICKHOUSE_PASSWORD`. Add `CLICKHOUSE_NAME` when you want custom host names.\nFor multi-host deployments, use comma-separated values in the same order across\nall four variables.\n\n```bash\ndocker compose up -d\nbun run docker:health\n```\n\n## Operations\n\n- Record where secrets are stored and who can rotate them.\n- Keep a rollback path for the previous image, Worker version, or deployment.\n- Watch app logs after deploy for ClickHouse connection errors.\n- Re-run the dependency audit after package updates:\n\n```bash\nbun audit\n```\n",
  "deploy/vercel": "# Vercel\n\nFor easy deployment, use the [Vercel Platform](https://vercel.com/new?utm_medium=default-template&filter=next.js&utm_source=create-next-app&utm_campaign=create-next-app-readme), created by the makers of Next.js. Refer to the [Next.js deployment documentation](https://nextjs.org/docs/deployment) for more details.\n\n[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new/clone?repository-url=https%3A%2F%2Fgithub.com%2Fduyet%2Fclickhouse-monitoring&env=CLICKHOUSE_HOST,CLICKHOUSE_USER,CLICKHOUSE_PASSWORD,CLICKHOUSE_MAX_EXECUTION_TIME,CLICKHOUSE_TZ,NEXT_QUERY_CACHE_TTL,NEXT_PUBLIC_LOGO,EVENTS_TABLE_NAME)\n\n### 2. Docker\n\nUse the published image from GitHub Container Registry and replace `vX.Y.Z`\nwith the release tag you want to run:\nhttps://github.com/duyet/clickhouse-monitoring/pkgs/container/clickhouse-monitoring\n\n```bash\ndocker run -it \\\n    -e CLICKHOUSE_HOST='http://localhost' \\\n    -e CLICKHOUSE_USER='default' \\\n    -e CLICKHOUSE_PASSWORD='' \\\n    -e CLICKHOUSE_TZ='Asia/Ho_Chi_Minh' \\\n    -e CLICKHOUSE_MAX_EXECUTION_TIME='15' \\\n    -e NEXT_QUERY_CACHE_TTL='86400' \\\n    --name clickhouse-monitoring \\\n    ghcr.io/duyet/clickhouse-monitoring:vX.Y.Z\n```\n\n### 3. Kubernetes Helm Chart\n\nUsing the latest helm chart here: https://github.com/duyet/charts/tree/master/clickhouse-monitoring\n\n```bash\nhelm repo add duyet https://duyet.github.io/charts\n\ncat <<EOF >> values.yaml\nenv:\n  - name: CLICKHOUSE_HOST\n    value: http://localhost:8123\n  - name: CLICKHOUSE_USER\n    value: default\n  - name: CLICKHOUSE_PASSWORD\n    value: ''\n  - name: CLICKHOUSE_TZ\n    value: 'Asia/Ho_Chi_Minh'\n  - name: CLICKHOUSE_MAX_EXECUTION_TIME\n    value: '15'\n  - name: NEXT_QUERY_CACHE_TTL\n    value: '86400'\nEOF\n\nhelm install -f values.yaml clickhouse-monitoring-release duyet/clickhouse-monitoring\n```\n\nimport { Cards } from 'nextra/components'\nimport { Triangle, Container, Blocks } from 'lucide-react'\n\n<Cards>\n  <Cards.Card icon={<Container />} title=\"Docker\" href=\"/deploy/docker\" arrow />\n  <Cards.Card icon={<Blocks />} title=\"Kubernetes\" href=\"/deploy/k8s\" arrow />\n</Cards>\n",
  "faq": "# FAQ\n\n## Which ClickHouse user should I use?\n\nCreate a dedicated monitoring user. Do not use an admin account for a shared\ndashboard. Start with read-only system-table access, then add grants for actions\nyou intentionally enable, such as `KILL QUERY` or `OPTIMIZE`.\n\n## Why are some pages empty?\n\nThe page may depend on a ClickHouse system table that is disabled, unavailable\nin your ClickHouse version, or empty on your cluster. Check the system table\n[setup guide](/docs/getting-started/clickhouse-enable-system-tables) and\nconfirm the monitoring user can query that table directly.\n\n## Why does the dashboard show stale data?\n\nThe app uses SWR in the browser and server-side query caching. Lower\n`NEXT_QUERY_CACHE_TTL` if freshness matters more than ClickHouse load.\n\n## Can I monitor multiple clusters?\n\nYes. Configure comma-separated `CLICKHOUSE_HOST`, `CLICKHOUSE_USER`,\n`CLICKHOUSE_PASSWORD`, and optional `CLICKHOUSE_NAME` values. The host selector\nuses the same index across all four variables.\n\n## Should I expose the AI agent publicly?\n\nThe agent is public by default on self-hosted deployments that do not enable\nClerk. The agent can query ClickHouse with the same grants as the configured\nmonitoring user, so evaluate whether your deployment needs restricted access.\n\nTo require authentication for the agent, set a feature override:\n\n```bash\nCHM_FEATURE_AGENT_ACCESS=authenticated\n```\n\nSee the [Feature Permissions](/docs/advanced/feature-permissions) guide for\nall restriction options, including MCP and action features.\n\n## Does the app modify ClickHouse data?\n\nMost pages are read-only. Some actions can modify operational state when the\nClickHouse user has grants, including kill query, optimize table, and\nself-tracking event writes. Disable or protect those features if you do not want\noperators to trigger them from the UI.\n\n## What deployment should I choose?\n\nUse Docker for the simplest self-hosted path. Use Cloudflare Workers when you\nwant edge hosting and already operate Cloudflare. Use Kubernetes when you need\nthe dashboard managed with the rest of your platform workloads.\n\n## Why does Docker not connect to localhost ClickHouse?\n\nInside a container, `localhost` points at the container itself. Use the\nClickHouse service name on a Docker network, or use the host gateway address\nfor a ClickHouse server running on the Docker host.\n\n## How do I rotate ClickHouse credentials?\n\nUpdate the ClickHouse user password first, then update the deployment secret,\nthen redeploy or restart the app. For Cloudflare, use `bun run cf:config` or\nupdate Worker secrets in the dashboard before redeploying.\n",
  "features": "# Features\n\nClickHouse Monitor is organized around the operational questions you answer\nwhile running a ClickHouse cluster.\n\n## Overview\n\nUse the overview page first when you want a fast health check. It groups\nconnections, queries, merges, replication, and system metrics into tabs so you\ncan spot the noisy area before drilling into a table.\n\n## Query Monitoring\n\nUse **Running Queries** for live work and **Query** pages for historical query\nlog analysis.\n\n- Sort by elapsed time, memory, read rows, read bytes, and selected columns.\n- Open query text from table rows when you need the exact statement.\n- Use EXPLAIN tools to inspect plans and pipelines for a selected query.\n- Use the kill action only with a ClickHouse user that has `KILL QUERY`.\n\n## Tables and Storage\n\nUse **Tables**, **Top Usage Tables**, and the explorer views when you need to\nunderstand storage growth.\n\n- Compare rows, bytes, parts, compression, and per-column sizes.\n- Use database and table filters before exporting large views.\n- Use table actions such as optimize only for tables where the monitoring user\n  has the required ClickHouse grants.\n\n## Clusters and Replication\n\nCluster pages focus on distributed operational state:\n\n- distributed queues\n- replica lag and replication health\n- background merges and mutations\n- host-level CPU, memory, disk, and network signals\n\n## Data Explorer\n\nThe explorer is for ad-hoc read-only inspection. It can list databases, tables,\ncolumns, dependencies, and preview rows. Keep previews small when working with\nlarge production tables.\n\n## AI Agent\n\nThe agent can help turn natural language into ClickHouse questions, explain\nquery behavior, and summarize findings. It still uses the same configured\nClickHouse host and user, so its visibility is limited by the grants you give\nthat user.\n\nRequired configuration:\n\n```bash\nLLM_API_KEY=your-provider-key\nLLM_API_BASE=https://your-provider-compatible-endpoint/v1\nLLM_MODEL=your-model\n```\n\nFor deployments that expose the agent publicly, configure feature permissions\nso agent access requires authentication.\n\nSee the [AI Agent](/docs/ai-agent) guide for the full list of tools, skills,\nand example questions.\n\n## MCP Server\n\nThe MCP endpoint lets external AI tools query the monitor through `/api/mcp`.\nProduction deployments require API key authentication. See the MCP guide in the\nadvanced docs before exposing this endpoint outside a trusted network.\n\n## Browser Connections\n\nBrowser connections let users store additional ClickHouse connection details in\ntheir browser and query through the app proxy. Use this for temporary inspection\nor personal workflows. For team dashboards, prefer server-side\n`CLICKHOUSE_HOST`, `CLICKHOUSE_USER`, and `CLICKHOUSE_PASSWORD` configuration.\n",
  "getting-started": "# Getting Started\n\nimport { Cards } from 'nextra/components'\nimport { Code, UserCog, Grid2x2Check, Settings } from 'lucide-react'\n\n<Cards>\n  <Cards.Card\n    icon={<Code />}\n    title=\"Local Development\"\n    href=\"/getting-started/local\"\n    arrow\n  />\n  <Cards.Card\n    icon={<UserCog />}\n    title=\"User Roles and Profile\"\n    href=\"/getting-started/clickhouse-requirements\"\n    arrow\n  />\n  <Cards.Card\n    icon={<Grid2x2Check />}\n    title=\"Enable System Tables\"\n    href=\"/getting-started/clickhouse-enable-system-tables\"\n    arrow\n  />\n  <Cards.Card icon={<Settings />} title=\"Settings\" href=\"/settings\" arrow />\n</Cards>\n",
  "getting-started/clickhouse-enable-system-tables": "# Enable System Tables\n\nClickHouse Monitoring relies on system log tables. Most are **enabled by default** in a fresh ClickHouse installation, but some deployments explicitly disable them with `<... remove=\"1\"/>` to save disk space. This page explains which tables the monitoring tool uses and how to enable them.\n\n## Required Tables\n\nThese tables are essential for core monitoring features:\n\n| Table | Purpose | Default |\n|-------|---------|---------|\n| `system.query_log` | Query history, performance analysis | Enabled |\n| `system.metric_log` | Historical server metrics (CPU, memory, network) | Enabled |\n| `system.asynchronous_metric_log` | Periodic async metrics | Enabled |\n| `system.part_log` | Merge/part operations history | Enabled |\n| `system.error_log` | Server error history | Enabled (v23.8+) |\n\n## Optional Tables\n\nThese enable advanced features. The monitoring tool gracefully handles their absence.\n\n| Table | Feature Unlocked | Default |\n|-------|-----------------|---------|\n| `system.query_thread_log` | Thread analysis, parallelization stats | Disabled¹ |\n| `system.processors_profile_log` | Query profiler | Disabled¹ |\n| `system.text_log` | Server text log viewer | Disabled¹ |\n| `system.session_log` | Login attempts, session history | Disabled¹ |\n| `system.backup_log` | Backup/restore history | Disabled² |\n| `system.zookeeper` | ZooKeeper/ClickHouse Keeper status | No ZooKeeper |\n\n¹ Requires `log_query_threads=1` in profile settings and explicit config entry.\n² Only exists when backup is configured.\n\n## Configuration\n\nCreate or edit `/etc/clickhouse-server/config.d/system-logs.xml`:\n\n```xml\n<clickhouse>\n  <!-- Required: core monitoring tables -->\n  <query_log>\n    <database>system</database>\n    <table>query_log</table>\n    <flush_interval_milliseconds>7500</flush_interval_milliseconds>\n    <max_size_rows>1048576</max_size_rows>\n    <reserved_size_rows>8192</reserved_size_rows>\n    <buffer_size_rows_flush_threshold>524288</buffer_size_rows_flush_threshold>\n    <flush_on_crash>true</flush_on_crash>\n  </query_log>\n\n  <metric_log>\n    <database>system</database>\n    <table>metric_log</table>\n    <flush_interval_milliseconds>7500</flush_interval_milliseconds>\n    <collect_interval_milliseconds>60000</collect_interval_milliseconds>\n    <max_size_rows>1048576</max_size_rows>\n    <reserved_size_rows>8192</reserved_size_rows>\n    <buffer_size_rows_flush_threshold>524288</buffer_size_rows_flush_threshold>\n    <flush_on_crash>true</flush_on_crash>\n  </metric_log>\n\n  <asynchronous_metric_log>\n    <database>system</database>\n    <table>asynchronous_metric_log</table>\n    <flush_interval_milliseconds>7500</flush_interval_milliseconds>\n    <collect_interval_milliseconds>60000</collect_interval_milliseconds>\n    <max_size_rows>1048576</max_size_rows>\n    <reserved_size_rows>8192</reserved_size_rows>\n    <buffer_size_rows_flush_threshold>524288</buffer_size_rows_flush_threshold>\n    <flush_on_crash>true</flush_on_crash>\n  </asynchronous_metric_log>\n\n  <part_log>\n    <database>system</database>\n    <table>part_log</table>\n    <flush_interval_milliseconds>7500</flush_interval_milliseconds>\n    <max_size_rows>1048576</max_size_rows>\n    <reserved_size_rows>8192</reserved_size_rows>\n    <buffer_size_rows_flush_threshold>524288</buffer_size_rows_flush_threshold>\n    <flush_on_crash>true</flush_on_crash>\n  </part_log>\n\n  <error_log>\n    <database>system</database>\n    <table>error_log</table>\n    <flush_interval_milliseconds>7500</flush_interval_milliseconds>\n    <collect_interval_milliseconds>1000</collect_interval_milliseconds>\n    <max_size_rows>1048576</max_size_rows>\n    <reserved_size_rows>8192</reserved_size_rows>\n    <buffer_size_rows_flush_threshold>524288</buffer_size_rows_flush_threshold>\n    <flush_on_crash>true</flush_on_crash>\n  </error_log>\n\n  <!-- Optional: advanced features -->\n  <query_thread_log>\n    <database>system</database>\n    <table>query_thread_log</table>\n    <flush_interval_milliseconds>7500</flush_interval_milliseconds>\n    <max_size_rows>1048576</max_size_rows>\n    <reserved_size_rows>8192</reserved_size_rows>\n    <buffer_size_rows_flush_threshold>524288</buffer_size_rows_flush_threshold>\n    <flush_on_crash>true</flush_on_crash>\n  </query_thread_log>\n\n  <processors_profile_log>\n    <database>system</database>\n    <table>processors_profile_log</table>\n    <flush_interval_milliseconds>7500</flush_interval_milliseconds>\n    <max_size_rows>1048576</max_size_rows>\n    <reserved_size_rows>8192</reserved_size_rows>\n    <buffer_size_rows_flush_threshold>524288</buffer_size_rows_flush_threshold>\n    <flush_on_crash>true</flush_on_crash>\n  </processors_profile_log>\n\n  <text_log>\n    <database>system</database>\n    <table>text_log</table>\n    <flush_interval_milliseconds>7500</flush_interval_milliseconds>\n    <max_size_rows>1048576</max_size_rows>\n    <reserved_size_rows>8192</reserved_size_rows>\n    <buffer_size_rows_flush_threshold>524288</buffer_size_rows_flush_threshold>\n    <flush_on_crash>true</flush_on_crash>\n  </text_log>\n</clickhouse>\n```\n\n## Enable `log_query_threads`\n\nFor `query_thread_log` to record data, set `log_query_threads` to `1` in your profile:\n\n```xml\n<!-- /etc/clickhouse-server/users.d/profiles.xml -->\n<clickhouse>\n  <profiles>\n    <default>\n      <log_query_threads>1</log_query_threads>\n    </default>\n  </profiles>\n</clickhouse>\n```\n\n## Re-enabling Disabled Tables\n\nIf your config has `<... remove=\"1\"/>` entries like:\n\n```xml\n<metric_log remove=\"1\"/>\n<asynchronous_metric_log remove=\"1\"/>\n<text_log remove=\"1\"/>\n<query_thread_log remove=\"1\"/>\n```\n\nRemove those lines and add the proper configuration block for each table (as shown above). The `<... remove=\"1\"/>` tag completely disables the table and prevents it from being created.\n\n## Verify\n\nAfter applying the config, restart ClickHouse and check:\n\n```bash\nsudo systemctl restart clickhouse-server\nclickhouse-client --query \"SELECT name FROM system.tables WHERE database = 'system' AND name LIKE '%_log' ORDER BY name\"\n```\n\n## Disk Usage\n\nSystem log tables can grow over time. To control disk usage:\n\n- **`max_size_rows`**: Limits table rows (default 1048576 ≈ 1M rows)\n- **TTL**: Add `TTL event_date + INTERVAL 30 DAY` to automatically prune old data\n- **`collect_interval_milliseconds`**: Higher values = less frequent collection (e.g., 60000 = every minute for metrics)\n",
  "getting-started/clickhouse-requirements": "# ClickHouse User Role and Profile\n\n## 1. Monitoring user role\n\nSuggested role for **monitoring** user must have these privileges on `system` database:\n\n```xml\n# File: users.d/monitoring_role.xml\n<clickhouse>\n  <users>\n    <monitoring>\n      <password><!-- define password here --></password>\n      <profile>monitoring_profile</profile>\n      <networks><ip>::/0</ip></networks>\n      <grants>\n        <query>GRANT monitoring_role</query>\n      </grants>\n    </monitoring>\n  </users>\n\n  <roles>\n    <monitoring_role>\n      <grants>\n        <query>REVOKE ALL ON *.*</query>\n        <query>GRANT SELECT,SHOW,OPTIMIZE,dictGet,REMOTE ON *.*</query>\n        <query>GRANT CREATE TEMPORARY TABLE ON *.*</query>\n        <query>GRANT CREATE KILL QUERY ON *.*</query>\n        <query>GRANT SELECT,INSERT,ALTER,CREATE,DROP,TRUNCATE ON system.monitoring_events</query>\n      </grants>\n    </monitoring_role>\n  </roles>\n</clickhouse>\n```\n\nPrivileges explains:\n\n- `CREATE TEMPORARY TABLE` is needed because the UI uses `FROM merge(system, '^query_log')`, which allows retrieving all the data from old tables that were renamed during the upgrade.\n- `OPTIMIZE` is needed for the table optimization action on Data Explorer.\n- `KILL QUERY` is needed for the \"Kill query\" action on Running Queries.\n- `INSERT`, `ALTER`, `CREATE`, `DROP`, and `TRUNCATE` are needed for the [feat: add pageview tracking #243](https://github.com/duyet/clickhouse-monitoring/pull/243) feature, which is [configurable](https://github.com/duyet/clickhouse-monitoring/pull/357). You should change `system.monitoring_events` to your own database and table.\n\n![Optimize Table Action](/grant-optimize.png)\n\n![Running Queries Action](/grant-kill.png)\n\n## 2. Monitoring user profile\n\n```xml\n# File: users.d/monitoring_profile.xml\n<clickhouse>\n  <profiles>\n    <monitoring_profile>\n      <allow_experimental_analyzer>1</allow_experimental_analyzer>\n\n      <!-- Optional: query cache to avoid hit too much queries on database -->\n      <use_query_cache>1</use_query_cache>\n      <query_cache_ttl>50</query_cache_ttl>\n      <query_cache_max_entries>0</query_cache_max_entries>\n      <query_cache_system_table_handling>save</query_cache_system_table_handling>\n      <query_cache_nondeterministic_function_handling>save</query_cache_nondeterministic_function_handling>\n    </monitoring_profile>\n  </profiles>\n</clickhouse>\n```\n",
  "getting-started/local": "import { Steps, Tabs, Cards } from 'nextra/components'\nimport { Code, UserCog, Grid2x2Check } from 'lucide-react'\n\n# Local Development\n\nTo get the project up and running on your local machine, follow these steps:\n\n<Steps>\n### Clone the repository\n\n```bash\ngit clone https://github.com/duyet/clickhouse-monitoring\n```\n\n### Install dependencies\n\n```bash\nbun install\n```\n\n### Setup env\n\nCreate a `.env.local` file by copying the `.env.example` file and filling in the required environment variables:\n\n- `CLICKHOUSE_HOST`: ClickHouse host, for example `http://localhost:8123`\n- `CLICKHOUSE_USER`: ClickHouse user with permission to query the `system` database.\n- `CLICKHOUSE_PASSWORD`: ClickHouse password for the specified user.\n- `CLICKHOUSE_MAX_EXECUTION_TIME`: [`max_execution_time`](https://clickhouse.com/docs/en/operations/settings/query-complexity#max-execution-time) timeout in seconds. Default is `10`.\n- `CLICKHOUSE_TZ`: ClickHouse server timezone. Default: `''`.\n- `NEXT_QUERY_CACHE_TTL`: TTL of [`unstable_cache`](https://nextjs.org/docs/app/api-reference/functions/unstable_cache) - cache the results of most charts to speed up and reuse them across multiple requests. Default: `86400`.\n- `NEXT_PUBLIC_LOGO`: (Optional) HTTP path to logo image.\n- `EVENTS_TABLE_NAME`: The table name for storing dashboard self-tracking events. Default: `system.monitoring_events`\n\n### Config ClickHouse\n\n<Cards>\n  <Cards.Card\n    icon={<UserCog />}\n    title=\"1. User Roles and Profile\"\n    href=\"/getting-started/clickhouse-requirements\"\n    arrow\n  />\n  <Cards.Card\n    icon={<Grid2x2Check />}\n    title=\"2. Enable System Tables\"\n    href=\"/getting-started/clickhouse-enable-system-tables\"\n    arrow\n  />\n</Cards>\n\n### Run the development server\n\n```bash\nbun run dev\n```\n\n### Final\n\nOpen [http://localhost:3000](http://localhost:3000) in your browser to see the dashboard.\n\n</Steps>\n",
  "": "import { Cards } from 'nextra/components'\nimport { Rocket, ChartNoAxesCombined } from 'lucide-react'\n\n# Introduction\n\nThe simple Next.js dashboard that relies on `system.*` tables to help monitor and provide an overview of your ClickHouse cluster.\n\nFeatures:\n\n- Query monitor: current queries, query history, query resources (memory, parts read, file_open, ...), most expensive queries, most used tables or columns, etc.\n- Cluster monitor: total memory/CPU usage, distributed queue, global settings, mergetree settings, metrics, etc.\n- Tables and parts information: size, row count, compression, part size, etc., at the column level detail.\n- Useful tools: Zookeeper data exploration, query EXPLAIN, kill queries, etc.\n- Visualization metric charts: queries and resource usage, number of merges/mutation, merge performance, query performance, etc.\n\n**Demo** (_The ClickHouse server running on my homelab so can be slow sometimes_):\n\n- [clickhouse-monitoring.vercel.app](https://clickhouse-monitoring.vercel.app)\n- [clickhouse.duyet.net](https://clickhouse.duyet.net)\n\n![](../../.github/screenshots/screenshot_1.png)\n![](../../.github/screenshots/screenshot_2.png)\n![](../../.github/screenshots/screenshot_3.png)\n![](../../.github/screenshots/screenshot_4.png)\n![](../../.github/screenshots/screenshot_5.png)\n![](../../.github/screenshots/screenshot_6.png)\n![](../../.github/screenshots/screenshot_7.png)\n![](../../.github/screenshots/screenshot_8.png)\n\n<Cards>\n  <Cards.Card\n    icon={<ChartNoAxesCombined />}\n    title=\"Getting Started\"\n    href=\"/getting-started\"\n  />\n  <Cards.Card icon={<Rocket />} title=\"Deployments\" href=\"/deploy\" />\n</Cards>\n",
  "reference/environment-variables": "# Environment Variables\n\nReference of all supported environment variables, grouped by category.\n\n---\n\n## ClickHouse Connection\n\nRequired. At minimum set `CLICKHOUSE_HOST`.\n\n| Variable | Default | Description |\n|---|---|---|\n| `CLICKHOUSE_HOST` | — (required) | Comma-separated ClickHouse host URLs. |\n| `CLICKHOUSE_USER` | `default` | Comma-separated usernames. |\n| `CLICKHOUSE_PASSWORD` | `\"\"` | Comma-separated passwords. |\n| `CLICKHOUSE_NAME` | — | Comma-separated custom labels shown in the host selector. |\n\nEach position across the comma-separated values maps to the same host index.\nAll four vars must have the same number of values.\n\n---\n\n## Query Execution\n\n| Variable | Default | Description |\n|---|---|---|\n| `CLICKHOUSE_MAX_EXECUTION_TIME` | `60` | Query timeout in seconds. |\n| `CLICKHOUSE_TZ` | server default | Time zone for date/time formatting. |\n| `NEXT_QUERY_CACHE_TTL` | `3600` | Server-side query cache TTL in seconds. |\n| `CLICKHOUSE_DATABASE` | `system` | Database for app-owned tables (events, dashboards). |\n| `EVENTS_TABLE_NAME` | `system.monitoring_events` | Full override for the self-tracking events table. |\n\n### Connection Pool\n\n| Variable | Default | Description |\n|---|---|---|\n| `CLICKHOUSE_POOL_SIZE` | `10` | Max concurrent ClickHouse clients per host config. |\n| `CLICKHOUSE_POOL_TIMEOUT` | `300000` | Idle client timeout in ms (5 min). |\n| `CLICKHOUSE_POOL_CLEANUP_INTERVAL` | `60000` | Stale-client cleanup interval in ms (1 min). |\n\n---\n\n## Feature Permissions\n\nAll features are **public and enabled by default**. Override only what needs\ndifferent behavior.\n\n| Variable | Default | Description |\n|---|---|---|\n| `CHM_CONFIG_FILE` | — | Path to TOML/YAML config file for feature overrides. |\n| `CHM_DISABLED_FEATURES` | — | Comma-separated feature ids to disable. |\n| `CHM_AUTH_REQUIRED_FEATURES` | — | Comma-separated feature ids that require authentication. |\n| `CHM_FEATURE_{ID}_ACCESS` | `public` | Per-feature access level: `public`, `guest`, `authenticated`. |\n| `CHM_FEATURE_{ID}_ENABLED` | `true` | Per-feature enabled/disabled flag. |\n\nReplace `{ID}` with an uppercase feature id: `AGENT`, `TABLES`, `METRICS`,\n`SETTINGS`, `ACTIONS`, `MCP`, `PEERDB`, `INSIGHTS`, `SECURITY`, `LOGS`,\n`CLUSTER`, `OPERATIONS`, `DASHBOARD`, `OVERVIEW`, `QUERIES`, `HEALTH`, `DOCS`,\n`ABOUT`.\n\n```bash\n# Examples\nCHM_FEATURE_AGENT_ACCESS=authenticated\nCHM_FEATURE_METRICS_ENABLED=false\nCHM_FEATURE_SETTINGS_ACCESS=authenticated\n```\n\nSee the [Feature Permissions](/docs/advanced/feature-permissions) guide for\nfull details.\n\n---\n\n## Authentication (Clerk)\n\nOnly needed when any feature is set to `access = \"authenticated\"`.\n\n| Variable | Default | Description |\n|---|---|---|\n| `CHM_AUTH_PROVIDER` | `none` | Auth provider: `none`, `clerk`. |\n| `NEXT_PUBLIC_AUTH_PROVIDER` | `none` | Client-side auth provider (mirror `CHM_AUTH_PROVIDER`). |\n| `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY` | — | Clerk publishable key (`pk_...`). |\n| `CLERK_SECRET_KEY` | — | Clerk secret key (`sk_...`). |\n| `CHM_API_KEY_SECRET` | — | Shared secret for `/api/v1/auth/api-key` and MCP auth. |\n\n---\n\n## AI Agent / LLM Provider\n\nThe AI agent uses an OpenAI-compatible API. At minimum set `LLM_API_KEY`.\n\n| Variable | Default | Description |\n|---|---|---|\n| `LLM_API_KEY` | — | Provider API key. |\n| `LLM_API_BASE` | `https://openrouter.ai/api/v1` | API base URL. |\n| `LLM_MODEL` | `openrouter:openrouter/free` | Model identifier. |\n\n### Provider-Specific\n\n| Variable | Default | Description |\n|---|---|---|\n| `ANYROUTER_API_KEY` | — | AnyRouter API key. |\n| `ANYROUTER_API_BASE` | — | AnyRouter API base URL. |\n| `OPENROUTER_API_KEY` | — | OpenRouter API key. |\n| `OPENROUTER_API_BASE` | — | OpenRouter API base URL. |\n| `OPENROUTER_REFERER` | — | OpenRouter referer header for rankings. |\n| `OPENROUTER_APP_NAME` | — | OpenRouter app name for rankings. |\n| `OPENROUTER_MODELS_API` | `https://openrouter.ai/api/v1/models` | OpenRouter models list endpoint. |\n| `NVIDIA_API_KEY` | — | NVIDIA NIM API key. |\n| `NVIDIA_API_BASE` | — | NVIDIA NIM API base URL. |\n\n### Agent Behaviour\n\n| Variable | Default | Description |\n|---|---|---|\n| `AGENT_API_TOKEN` | — | Shared token for agent API Bearer auth. |\n| `AGENT_ENABLE_CONTROL_TOOLS` | `false` | Enables kill-query, optimize, and other write actions. |\n\n---\n\n## Conversation Store\n\n| Variable | Default | Description |\n|---|---|---|\n| `NEXT_PUBLIC_FEATURE_CONVERSATION_DB` | `false` | Enable database-backed conversation storage. Requires Clerk. |\n| `DATABASE_URL` | — | PostgreSQL connection string for conversation store. |\n| `POSTGRES_URL` | — | Alternative PostgreSQL connection string. |\n| `POSTGRES_PRISMA_URL` | — | Prisma-specific PostgreSQL connection string. |\n\n---\n\n## PeerDB Monitoring\n\nOptional, view-only. Set `PEERDB_API_URL` to enable the **PeerDB** section\n(Mirrors and Peers) in the sidebar. When unset, the section shows a\n\"not configured\" state and the proxy returns `503`.\n\n| Variable | Default | Description |\n|---|---|---|\n| `PEERDB_API_URL` | — | Base URL of the PeerDB REST API. For the PeerDB **UI** (NextAuth), include the `/api` suffix — e.g. `https://peerdb.example.com/api` — since it proxies `/api/v1/*` to the flow-api. For a raw **flow-api**, use the bare origin — e.g. `http://localhost:8113`. |\n| `PEERDB_PASSWORD` | — | PeerDB UI/API password. Sent as HTTP Basic with an empty username (`base64(\":\" + password)`). Leave empty if the API has no auth. Server-side only — never exposed to the browser. |\n\nCHM proxies only a **read-only allowlist** of PeerDB endpoints\n(`app/api/v1/peerdb/[...slug]`); mutating endpoints (create/drop/pause,\nalert config, maintenance) are rejected with `403`.\n\n```bash\n# Example: PeerDB UI behind NextAuth\nPEERDB_API_URL=https://peerdb.example.com/api\nPEERDB_PASSWORD=your-peerdb-ui-password\n\n# Example: raw flow-api (no auth)\nPEERDB_API_URL=http://localhost:8113\n```\n\nFor local development without a PeerDB instance, run the bundled mock:\n\n```bash\nbun run peerdb:mock                                  # serves :8113\nPEERDB_API_URL=http://localhost:8113 bun run dev\n```\n\n---\n\n## Analytics & Branding\n\n| Variable | Default | Description |\n|---|---|---|\n| `NEXT_PUBLIC_TITLE_SHORT` | `ClickHouse` | Browser tab title (short form). |\n| `NEXT_PUBLIC_LOGO` | — | Custom logo URL (header branding). |\n| `NEXT_PUBLIC_AUTOCOMPLETE_LIMIT` | — | Max results in autocomplete dropdowns. |\n| `NEXT_PUBLIC_MEASUREMENT_ID` | — | Google Analytics measurement ID (`G-...`). |\n| `NEXT_PUBLIC_SELINE_ENABLED` | `false` | Enable Seline analytics. |\n| `NEXT_PUBLIC_VERCEL_ANALYTICS` | `false` | Enable Vercel Analytics. |\n| `NEXT_PUBLIC_POSTHOG_KEY` | — | PostHog project API key. |\n| `NEXT_PUBLIC_POSTHOG_HOST` | `https://us.i.posthog.com` | PostHog host URL. |\n\n---\n\n## Runtime & Build\n\n| Variable | Default | Description |\n|---|---|---|\n| `NODE_ENV` | `development` | Runtime environment (`development`, `production`, `test`). |\n| `ENABLE_CLOUDFLARE` | `false` | Enable Cloudflare-specific build configuration. |\n| `CLOUDFLARE_WORKERS` | — | Set to `1` when running on Cloudflare Workers. |\n| `CF_PAGES` | — | Set by Cloudflare Pages at runtime. |\n| `MINIFLARE` | — | Set to `1` when running locally with Miniflare. |\n| `DOCS_CONTENT_ROOT` | — | Override docs content source directory. |\n\n### Build-Time\n\nThese are injected at build time for the About page:\n\n| Variable | Description |\n|---|---|\n| `NEXT_PUBLIC_GIT_SHA` | Current commit SHA. |\n| `NEXT_PUBLIC_GIT_REF` | Current git branch or tag. |\n| `NEXT_PUBLIC_BUILD_TIMESTAMP` | ISO build timestamp. |\n| `NEXT_PUBLIC_CI` | Set to `true` in CI environments. |\n\n---\n\n## Legacy / Migration\n\n| Variable | Default | Description |\n|---|---|---|\n| `CLICKHOUSE_CACHE_TAG` | — | Legacy cache tag override (prefer `NEXT_QUERY_CACHE_TTL`). |\n| `DATABASE_URL` | — | Migration / conversation store database URL. |\n| `POSTGRES_URL` | — | Migration / conversation store database URL (fallback). |\n",
  "settings": "# Settings\n\nSettings are controlled with environment variables and, for feature access, an\noptional config file.\n\n## Required ClickHouse Settings\n\n```bash\nCLICKHOUSE_HOST=https://your-clickhouse-host:8443\nCLICKHOUSE_USER=monitoring\nCLICKHOUSE_PASSWORD=change-me\n```\n\nFor multiple hosts, use comma-separated values in the same order:\n\n```bash\nCLICKHOUSE_HOST=https://prod-a:8443,https://prod-b:8443\nCLICKHOUSE_USER=monitoring,monitoring\nCLICKHOUSE_PASSWORD=secret-a,secret-b\nCLICKHOUSE_NAME=prod-a,prod-b\n```\n\n## Query Runtime\n\n```bash\nCLICKHOUSE_MAX_EXECUTION_TIME=60\nCLICKHOUSE_TZ=UTC\nNEXT_QUERY_CACHE_TTL=300\n```\n\nUse a shorter cache TTL when you need fresher operational data. Use a longer\nTTL for shared dashboards where lower ClickHouse load matters more than\nsecond-by-second freshness.\n\n## Feature Permissions\n\nAll features are public and enabled by default. You only need to configure\nfeatures you want to restrict or disable.\n\n```bash\n# Make agent require authentication\nCHM_FEATURE_AGENT_ACCESS=authenticated\n\n# Disable settings entirely\nCHM_FEATURE_SETTINGS_ENABLED=false\n\n# Require auth for multiple features at once\nCHM_AUTH_REQUIRED_FEATURES=agent,mcp,settings,actions\n```\n\nFor complex setups, use a TOML or YAML config file:\n\n```bash\nCHM_CONFIG_FILE=/etc/clickhouse-monitor/config.toml\n```\n\n```toml\n[features.agent]\naccess = \"authenticated\"\n\n[features.settings]\nenabled = false\n\n[features.mcp]\naccess = \"authenticated\"\n```\n\nSee [Feature Permissions](/docs/advanced/feature-permissions) for all available\noptions.\n\n## Clerk Authentication\n\nOnly needed when a feature is set to `access = \"authenticated\"`:\n\n```bash\nCHM_AUTH_PROVIDER=clerk\nNEXT_PUBLIC_AUTH_PROVIDER=clerk\nNEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_live_xxx\nCLERK_SECRET_KEY=sk_live_xxx\n```\n\n## AI Provider\n\nThe AI agent uses an OpenAI-compatible API.\n\n```bash\nLLM_API_KEY=your-provider-key\nLLM_API_BASE=https://your-provider-compatible-endpoint/v1\nLLM_MODEL=your-model\n```\n\nKeep server-side API keys out of `NEXT_PUBLIC_` variables.\n\n## Public Branding\n\n```bash\nNEXT_PUBLIC_LOGO=https://example.com/logo.svg\nNEXT_PUBLIC_VERCEL_ANALYTICS_ENABLED=false\nNEXT_PUBLIC_MEASUREMENT_ID=G-XXXXXXXXXX\nNEXT_PUBLIC_SELINE_ENABLED=false\n```\n\nOnly use `NEXT_PUBLIC_` for values that are safe to expose to every browser.\n",
}
